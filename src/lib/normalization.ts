/**
 * Re-export from matching/normalize.ts for backward compatibility.
 */
export {
  normalizeClub,
  normalizeToken,
  normalizeWhitespace,
  parsePersonName,
  stripDiacritics,
} from "@/matching/normalize.ts";
