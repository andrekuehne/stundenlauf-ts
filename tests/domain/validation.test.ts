import { describe, it } from "vitest";

describe("event validation", () => {
  it.todo("rejects duplicate person_id");
  it.todo("rejects team referencing unregistered persons");
  it.todo("rejects duplicate import_batch_id");
});
